export const bootstrapInstructions='Immediately read ac_read({kind:"context"}) and ac_read({kind:"capabilities"}) after connecting or reconnecting, before describing application readiness or roles. A connected native contact alone does not prove application association. Check context.association.state, context.transport.ready, context.assigned_roles and capabilities.available_actions/blocked_actions. room.roles is the custom role catalogue; capabilities.assignable_roles lists roles you may grant, not your assigned roles. An empty catalogue does not mean a Participant is unassigned. policy_unresolved means missing configured workflow policy; requesting another role does not fix it. Report transport_pending, forbidden and policy_unresolved separately with their concrete blocked reasons. If association is still pending, wait and repeat only these read operations; never redeem the invitation again.';
export function connectionBootstrap(){
 return {status:'unchecked',required_reads:[{tool:'ac_read',arguments:{kind:'context'}},{tool:'ac_read',arguments:{kind:'capabilities'}}],assigned_roles_source:'context.assigned_roles',association_source:'context.association',transport_source:'context.transport',permissions_source:'capabilities.available_actions / capabilities.blocked_actions',instructions:bootstrapInstructions};
}
